ability to load csv with a series of experiments - containing different sets of parameters
ability to specify in a gui variables for an experiment and randomize parameters (exports csv)

ability to use $VAR1 in the yaml file to denote variables which will be substituted during
yaml load

for each line in a csv create a result with timings of each command


in the yaml editor show all the possible options and command (like in Kile)





start1:
        desc: Optional description
        text:
                text: Please press spacebar to continue
                size: 16
                align: center
        wait:
                keypress: space
slide1:
        depends: start1
        